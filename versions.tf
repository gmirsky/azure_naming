terraform {
  # Specify the required Terraform version
  required_version = ">= 1.0"
  # Define required providers
  required_providers {
    # This module doesn't require any specific providers
    # But we declare this block for consistency
  }
}
